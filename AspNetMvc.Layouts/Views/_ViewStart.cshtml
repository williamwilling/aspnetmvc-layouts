@{
	// The layout property points to the file that contains the layout for a view. The ~ means that
	// the path is relative to the root of the application.	Since _ViewStart.cshtml is processed at
	// the start of every view, the layout that is defined here is used for all views (unless a view
	// specifically overrides the Layout-property).
	Layout = "~/Views/Shared/_Layout.cshtml";
}